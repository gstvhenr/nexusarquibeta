-- ==========================================================================
-- Migration 001 — Schema Inicial
-- ==========================================================================
-- Esta migration é um placeholder que representa o schema v1.
-- O schema.sql já cria todas as tabelas na inicialização.
-- Este arquivo existe para documentar que v1 = schema.sql original.
-- ==========================================================================

-- Nenhuma ação necessária: schema.sql já aplica o schema v1.
SELECT 1;
